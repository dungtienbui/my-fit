export enum MetricType {
  STEPS = 'steps', // Số bước đi
  CALORIES = 'calories', // Lượng calo tiêu thụ
  WATER = 'water', // Lượng nước uống (ml)
  EXERCISE = 'exercise', // Hoạt động thể dục (đi bộ, chạy, đạp xe, giá trị là khoảng cách: m)
  WEIGHT = 'weight', // Cân nặng (kg)
  HEIGHT = 'height', // Chiều cao (cm)
  SLEEP = 'sleep', // Giấc ngủ (phút)
}
